import java.util.Vector;

public class DomainController {
    private Database db;
    private Vector<String> currentlyLogged;

    //get currentlyLogged

    //check if register

    //handle user (opCode, userName, password)
        //resolve opCode
        //check if register
        //check if logged
        //return right result

    //register new user
        //add user to db

    //log in
        //add user to currently log
}
